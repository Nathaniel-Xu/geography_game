{
  "name": "atlas-drill",
  "version": "1.0.0",
  "private": true,
  "description": "Map-based drill for learning where all 195 countries are.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test test/",
    "build:data": "node scripts/build-data.mjs",
    "cloud:login": "spacetime login",
    "cloud:publish": "spacetime publish --server maincloud --module-path server geography-game",
    "cloud:publish:local": "spacetime publish --server local --module-path server geography-game",
    "cloud:bindings": "spacetime generate --lang typescript --out-dir server/module_bindings --module-path server && bun build server/module_bindings/index.ts --target browser --format esm --outfile vendor/spacetimedb/bindings.js",
    "cloud:logs": "spacetime logs --server maincloud geography-game"
  },
  "dependencies": {
    "cuelume": "0.2.2"
  }
}
